version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell      # the port arrived carrying "excpetion" in four places
    - revive
    - staticcheck
    - unused

  settings:
    # No locale is set on purpose. Go and .NET both use American spelling, and
    # Normalize is a required API name; forcing a locale flags those as typos.
    # Plain misspellings such as "excpetion" -- which this port arrived carrying
    # in four places -- are caught regardless.
    revive:
      rules:
        - name: exported
        - name: package-comments
        - name: var-naming
        - name: indent-error-flow

  exclusions:
    rules:
      # calc.go is a deliberate line-by-line port of .NET's DecCalc. Keeping it
      # recognisable against the reference is worth more than satisfying style
      # rules about naming and control flow, and the golden tables are what
      # actually holds it correct.
      - path: calc\.go
        linters: [revive, staticcheck]
      # The custom-format writer is likewise a direct port.
      - path: format\.go
        text: "cyclomatic|cognitive"

formatters:
  enable:
    - gofmt
